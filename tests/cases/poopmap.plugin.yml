- uses: kjanat/metrics@latest
  with:
    token: NOT_NEEDED
    plugin_poopmap_token: MOCKED_TOKEN
    plugin_poopmap: 'yes'
    use_mocked_data: 'yes'
    verify: 'yes'
  name: 💩 PoopMap plugin - (unnamed)
