<%
  // Base header partial - forwards to compact header
  // This maintains compatibility with standard partial naming
%>
<%- await include('header.compact.ejs', {yOffset, width, padding, lineHeight, compactSpacing, compact}) %>
