<%
  // Languages partial - forwards to compact languages
  // This maintains compatibility with standard partial naming
%>
<%- await include('languages.compact.ejs', {yOffset, width, padding, lineHeight, compactSpacing, compact}) %>
